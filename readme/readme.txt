plugin for CudaText.
gives command to switch from the current file (with several lexers: C, C++, Assembly) to its pair file, usually from source-code file to header-file.

plugin has file maps.py, which is config for file extensions.
e.g. it tells to switch: .cpp -> .h -> .hpp

author: Alexey (CudaText)
license: MIT
